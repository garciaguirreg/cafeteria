{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome (Live Server)",
            "url": "http://127.0.0.1:5500//", // Ajusta el puerto si es diferente
            "webRoot": "${workspaceFolder}"
        }
    ]
}